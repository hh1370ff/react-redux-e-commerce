import { Typography } from "@mui/material";
import React from "react";

const Logo = () => {
  return (
    <Typography variant="h2" sx={{ width: "20%" }}>
      One-Stop Shop
    </Typography>
  );
};

export default Logo;
